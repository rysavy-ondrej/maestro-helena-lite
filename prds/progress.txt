[2026-09-03 11:22:03 UTC] task 00 FAILED    0s exit=1 session=cc998df1-0d74-495e-b6d2-8e242255bd43
    [D0 Foundations] Bootstrap the single Python package and toolchain
    Session ended with exit code 1 and wrote no report.
    NEXT: The previous session produced no report. Check prds/logs/ before assuming nothing was changed on disk.

[2026-09-03 11:22:57 UTC] task 00 FAILED    0s exit=1 session=dfc7fbb1-24bb-46ee-be5a-1b9681529c9d
    [D0 Foundations] Bootstrap the single Python package and toolchain
    Session ended with exit code 1 and wrote no report.
    NEXT: The previous session produced no report. Check prds/logs/ before assuming nothing was changed on disk.

[2026-09-03 11:41:52 UTC] task 00 COMPLETED 767s exit=0 session=4f74c7e1-43e3-4bf6-8f0e-beda56d81eeb
    [D0 Foundations] Bootstrap the single Python package and toolchain
    The repository skeleton now exists: a uv project pinned to Python 3.12 exactly with a committed uv.lock, one package `helena` in a src/ layout with one documented module per architecture component, one pytest suite of 21 tests mirroring it, a Makefile, three decision records and a README that names concept/ as authoritative. Lint and typecheck tooling was NOT added — it is a dev dependency beyond the approved set, so it is recorded as an escalation and the two Makefile targets fail loud pointing at the decision record.
    ESCALATION [dependency] Lint and typecheck tooling. The task step asked for lint and typecheck targets; a linter and a type checker are dev dependencies beyond the approved set (CONTEXT.md §3: Pydantic, dotenv, a Kafka client, a PostgreSQL driver, pytest — anything else is an escalation). Not added. `make lint` and `make typecheck` exist and exit 1 naming docs/decisions/0003-lint-and-typecheck-tooling.md, which states what a decision needs to settle. `make check` covers what needs no new dependency: lockfile sync, compileall, the suite.
    ESCALATION [dependency] Kafka client choice within the already-approved slot. `confluent-kafka` was chosen over `kafka-python`, on the basis of a probe against the pinned bin/blink binary rather than on reputation. Recorded in docs/decisions/0002-dependency-set.md. Flagged because 'a Kafka client' was approved generically and a later session may want the specific choice revisited — in particular if consumer-group subscribe() turns out to be needed and blink does not support it.
    CONFLICT {'detail': "Task step 3 ('a justfile/Makefile with lint, test and typecheck targets') conflicts with prds/CONTEXT.md §3 and concept/instruction.md §3, which make any dependency beyond the baseline an escalation. The higher rule was followed: the targets exist and fail loud rather than the tools being added or the targets being silently omitted.", 'resolution': 'Escalated, not improvised. See docs/decisions/0003.'}
    CONFLICT {'detail': "Minor, and resolved conservatively: task step 1 says 'commit uv.lock'. There is no git repository in the tree and CONTEXT.md §5 says not to create commits unless the task says to. 'Commit' was read as 'uv.lock is a committed artifact, not a generated one' — it exists, is not gitignored, and `uv lock --check` is part of `make check`. No `git init` and no commit was made. If the operator meant a literal git repository, that is a one-command follow-up, but the choice of what belongs in an initial commit (prds/, implement.sh, data/) is theirs, not a session's."}
    NEXT: The skeleton is in place and `uv run pytest -q` is green (21 tests). Work through `uv run` and `uv add` against the existing .venv; `make check` is the gate. Two tests will police you: tests/test_dependency_boundary.py derives the approved import set from pyproject.toml and fails on any unapproved top-level import in src/helena (langchain, openai, httpx, requests, sqlalchemy and kafka-python are on an explicit absent list — the model client enters in the increment that first CALLS a model, not the one that mentions it), and tests/test_package_layout.py requires exactly one 'Maturity: <label>' line in every module docstring and that the module set equals the seven architecture components. Adding an eighth module means editing COMPONENT_MODULES in that test, which is intentional friction — a new component is an architecture decision. Every module is currently labelled 'Maturity: deferred — placeholder'; the task that implements one must change its label. Note also: bin/README.md references ../.gitignore and scripts/build-binaries.sh — the .gitignore now exists (this task wrote it), but scripts/build-binaries.sh does not, so that reference is still dangling. And an unrelated `secrets/` directory appeared at the project root during the session (not created by this task); it has been added to .gitignore defensively and its contents were not read. The concept/05 vs data/netify tension noted in CONTEXT.md §3 was not touched by this task and remains open.

[2026-09-03 12:01:15 UTC] task 01 COMPLETED 584s exit=0 session=79bce763-af84-4ccf-8ff9-50500c0ac10f
    [D0 Foundations] Fail-loud configuration and secret handling
    `helena.config` now exists: a `Settings.load()` that reads the process environment layered over an uncommitted `.env`, resolves model settings agent-specific → general → fail, and raises `ConfigurationError` naming every missing variable at once — with empty and whitespace-only counting as missing and no default anywhere (asserted structurally: no field has a default and the module holds no `://` literal). Credentials are a `Secret` wrapper that renders `***redacted***` in `str`, `repr`, `model_dump()` and `model_dump_json()`. `.env.example` lists all fifteen variable names with empty values and a test holds it identical to the loader's set.
    ESCALATION [configuration-scope] VIRUSTOTAL_AUTH_KEY is REQUIRED, although VirusTotal is a deferred provider that nothing calls. Reasoning in docs/decisions/0004: deferring the call is not deferring the configuration, the concept requires the example to document every variable name, and the key is live in the operator's .env. The cost is that a deployment without a VirusTotal key cannot start. Making it optional would create the first optional non-override variable, which is a decision rather than an edit — flagged here so it is a choice someone made and not an accident.
    ESCALATION [test-enforcement] tests/test_package_layout.py asserted the module set equalled the seven architecture components exactly, so adding config.py failed it. Rather than pretending configuration is an eighth component, the test now subtracts a named SUPPORT_MODULES = {"config"} set; an unexpected module still fails the assertion, so the friction task 0 built is intact. concept/03-architecture.md lists the environment as an interface, not a component, which is why config is not in COMPONENT_MODULES.
    ESCALATION [local-file] HELENA_TENANT and HELENA_SENSOR did not exist in .env — the operator's file carries the nine variables CONTEXT.md §3 lists, none of them the ingestion identity. Two non-secret lines were appended to the local .env (values 'local-lab' and 'local-sensor-01') so the loader resolves on this machine. No existing line was read or modified. If the operator wants different identifiers, changing them is a one-line edit; nothing in the code knows those values.
    NEXT: Read configuration through `helena.config.Settings.load()` and nothing else — no os.environ, no dotenv call, no second loader. The typed sections are settings.identity (tenant, sensor), settings.providers (abusech_auth_key, virustotal_auth_key — both Secret), settings.infrastructure (risingwave_dsn, kafka_bootstrap_servers) and settings.triage / settings.analyst (agent, endpoint_url, token, model, source). Adding a variable means adding it to helena.config.VARIABLES AND to .env.example, or tests/test_config.py fails — that is deliberate. A credential is a Secret; call .reveal() at the point of use and never anywhere near a log line, and if you put a Secret in a new Pydantic model, copy the ConfigDict (frozen, arbitrary_types_allowed, extra='forbid', hide_input_in_errors). Two things this task did NOT do and the next sessions own: redacting a key that travels inside a URL (task 2), and the RISINGWAVE_DSN, which is typed as a plain str because a RisingWave DSN is an address — if a deployment ever puts a password in it, that type is wrong and it should become a Secret. Still open from task 0 and untouched here: the concept/05 vs data/netify tension, and bin/README.md's dangling reference to scripts/build-binaries.sh.

[2026-09-03 12:12:53 UTC] task 02 COMPLETED 697s exit=0 session=a6dab420-abad-41d0-bbc0-3c7f5d963b64
    [D0 Foundations] Structured local logging with credential redaction
    `helena.observability` now exists: one log channel writing one JSON object per line to stderr with ten fixed top-level keys (timestamp, level, component, event, tenant, sensor, event_id, context_id, versions, fields), identity taken from Settings with no default, and a `Redactor` that removes credentials by registered value and by URL structure. Redaction runs at the emitter, not at the call site — every value is swept before serialization, `outbound_request` and `exception` route the request URL through `Redactor.url`, and the serialized line is swept once more before it is written. A boundary test keeps every hosted tracing/telemetry SDK undeclared, unimported and unresolvable in the environment, and a second test forbids `print()` and stdlib `logging` inside the package so there is one logging path.
    ESCALATION [test-enforcement] tests/test_package_layout.py SUPPORT_MODULES gained "observability" alongside "config". Observability is not an eighth architecture component: concept/07-principles.md gives it its own section rather than a place in the pipeline, and every component logs. The friction task 0 built is intact — an unexpected module still fails the assertion.
    ESCALATION [boundary-widening] tests/test_dependency_boundary.py gained HOSTED_TELEMETRY_DISTRIBUTIONS (17 entries: langsmith, langfuse, opentelemetry-{sdk,api}, sentry-sdk, ddtrace, datadog, newrelic, elastic-apm, logfire, libhoney, openinference-instrumentation, arize-phoenix, braintrust, wandb, mlflow, helicone) and two tests. The list is a judgement call about which SDKs count as hosted telemetry; prometheus-client was deliberately left off because a local scrape endpoint is not an egress channel. The stronger of the two tests asserts the modules do not RESOLVE in the environment at all, not merely that they are undeclared — so a tracer arriving transitively fails the suite. If a future approved dependency drags one in, that test is where it surfaces, and the answer is a recorded decision, not an exemption.
    ESCALATION [external-fact] CONTEXT.md §3 and concept/05-threat-intelligence.md both state that the abuse.ch key travels in the URL path for the bulk export. Checked against the artifact: https://threatfox.abuse.ch/export/json/recent/ returns 200 with NO credential, and both plausible key-in-path shapes return 404. So the specific claim about that endpoint does not hold today. The redaction rule is retained unchanged and is not weakened by this — the concept records that a live key already reached a project conversation inside a pasted link, and HTTPError.url leaks whatever URL you requested regardless of who requires what. But the feed-loader increment must establish the current bulk endpoint and its auth mechanism for itself rather than inherit the assumption. Not blocking: this task's job was the redactor, not the loader.
    NEXT: Log through `helena.observability.logger(component, settings)` and nothing else — `print()` and stdlib `logging` inside `helena` are a test failure (tests/test_observability.py::test_the_package_has_no_second_logging_path), and so is importing or installing any hosted tracing SDK (tests/test_dependency_boundary.py, two tests). The record has ten fixed top-level keys; your own fields are keyword arguments and land nested under `fields`, so you cannot shadow `tenant`. `event` is the dotted name of what happened; `event_id` is the normalizer's flow-event identity and `context_id` the host-context identity, both keyword arguments, both null when absent. `versions={}` raises — pass the real version set or omit it. A field that will not JSON-serialize raises TypeError rather than being repr'd, so convert datetimes at the call site. For anything with a URL: `log.outbound_request(event, method=..., url=...)` and `log.exception(event, exc, ...)` — never `log.info(..., url=raw_url)`, even though the emitter would catch a configured credential anyway. `Redactor.url(...)` is also the helper for the provenance URL a loader stores, so the row and the log line get identical treatment; build one with `Redactor.from_settings(settings)`. Two things the next sessions own that this task surfaced: the abuse.ch bulk endpoint really does not take the key in the path today (see escalations — fetch before building the loader), and stdlib `logging` from psycopg/confluent-kafka is unrouted. Still open and untouched since task 0: the concept/05 vs data/netify tension, bin/README.md's dangling reference to scripts/build-binaries.sh, and there is still no git repository.

[2026-09-03 12:44:57 UTC] task 03 COMPLETED 1922s exit=0 session=d8b93ba8-ad76-4dae-9623-c4ad1eaf76c0
    [D0 Foundations] Pin and run the broker and streaming engine
    The pinned engine and broker are now runnable and checkable from the repository: `scripts/dev-up` / `scripts/dev-down` start and stop them on the addresses `helena.config` resolves, `docs/versions.md` carries the sha256 and version-string pins, `scripts/dev_check.py` is the one definition of "the pins match and both endpoints answer", and `tests/test_infrastructure.py` (9 tests, ordered ahead of every other integration test) executes it — including a round trip through the broker over the Kafka wire protocol and a psycopg query against a real engine. The libpython ABI hazard was measured rather than repeated, and `docs/runbook.md` §1 records the result: there is no runtime symptom at all, so the guard is structural (ldd plus a checksum on the resolved file) and has its own test.
    ESCALATION [conflict] The PRD step said dev-up should FETCH the pinned Blink commit and RisingWave release. prds/CONTEXT.md §3 is higher authority and says the opposite — do not build, download, containerise or depend on these binaries. Implemented as verify-and-run: dev-up checks what is on disk against docs/versions.md and fails loud naming the file if a binary is absent or does not match. Recorded in docs/decisions/0006 and in docs/versions.md. The honest consequence: there is still NO automated way to get the binaries onto a fresh node, and bin/README.md's `scripts/build-binaries.sh` still does not exist. Writing it is separate work and would need a decision about downloading.
    ESCALATION [egress] RisingWave 3.0.3 reports to telemetry.risingwave.dev by default (observed in .run/risingwave.log before the override, as HTTP 500s from its telemetry reporter). concept/06-technology.md puts the whole automatic pipeline on the local node and ADR-0005 already refused hosted tracing as a second egress channel, so scripts/risingwave.toml sets `[server] telemetry_enabled = false` and nothing else. Flagging it because it is an outbound channel decision made inside a task, and because it was ON in every engine run any earlier session did by hand.
    ESCALATION [state file] scripts/dev-up writes pidfiles and logs under `.run/` (gitignored). This is process management for a dev script, not a store — no project data, nothing durable, nothing read by the pipeline — but the single-store rule is strict enough that it is worth naming rather than assuming. If that reading is wrong the alternative is matching processes by command line, which this session found actively dangerous (see failed_approaches).
    ESCALATION [open, untouched] The concept/05 (Netify commercial-only, out of scope) vs data/netify (a million rows on disk) tension noted in CONTEXT.md §3 is still open. This task did not touch enrichment and did not pick a side.
    CONFLICT PRD step 1 ("fetches and runs") vs prds/CONTEXT.md §3 ("Do not build these, download them, containerise them, or add them as dependencies"). Resolved in favour of CONTEXT.md; dev-up verifies and runs, never downloads. See the first escalation and docs/decisions/0006.
    NEXT: Infrastructure is a solved problem for you: write a test, take the `engine_dsn` or `broker_bootstrap` fixture from tests/conftest.py, mark it `@pytest.mark.integration`, and an engine or broker will be there — started in-memory by the fixture if nothing is running, reused if `scripts/dev-up` has one. Do not start processes inside your own test. `uv run pytest -q` is green either way (130 tests, ~12 s from cold). Read docs/runbook.md before you touch bin/ or write a consumer; its §1 is the libpython hazard with the measurement, and §3 is why the broker must be consumed with `assign()` and never `subscribe()`. Three things that will bite otherwise: (1) `SELECT version()` becomes true about 0.15 s before DDL does, so if your migration runner fires immediately after startup, retry the DDL rather than trusting the endpoint check; (2) only one RisingWave can run per machine because meta and compute bind fixed ports, so `scripts/dev-down` before debugging a bind failure; (3) never use `pgrep -f`/`pkill -f` with a pattern that appears in your own prompt or command line — it kills your own session, which happened twice here. If you change a pinned binary you must change `docs/versions.md`; `scripts/dev_check.py` is the only code that reads it and both dev-up and the suite fail loud on a mismatch. Still open and untouched: `scripts/build-binaries.sh` does not exist and nothing in the repository downloads a binary (deliberate, ADR-0006); the concept/05 vs data/netify tension; and there is still no git repository, so nothing in this task was committed.

[2026-09-03 12:59:55 UTC] task 04 COMPLETED 897s exit=0 session=c6514a87-9095-43d8-8884-9d150c07e76d
    [D0 Foundations] SQL migration runner over numbered files
    The engine's schema is now project source: `sql/migrations/NNNN_name.sql` applied in order by `helena.migrations`, which records every application in `helena_schema_migrations` in the engine and otherwise only refuses — on a gap, a duplicate number, a missing file, a rename, any checksum change to an applied file, a recorded `failed`, or a connection that is not in autocommit. `scripts/migrate.py` runs it by hand, `tests/conftest.py::migrated_engine` runs it for the suite, and 25 new tests execute all of it against a real RisingWave, including the half-migrated failure path. No new dependency: psycopg was already here, and no SQL transformation framework was introduced.
    ESCALATION [layout] `helena.migrations` was added as a third SUPPORT_MODULE in tests/test_package_layout.py, beside `config` and `observability`. That test invites the edit deliberately ('a deliberate edit rather than a new file appearing unnoticed') and it is not an architecture component, so this is recorded rather than escalated: applying the schema belongs to no single stage, and concept/03-architecture.md makes the engine's view and model definitions project source. Reasoning is in docs/decisions/0007-sql-migrations.md.
    ESCALATION [documentation-tension] Still open and untouched by this task, carried forward from tasks 0-3: concept/05-threat-intelligence.md records Netify as commercial-only and out of scope while data/netify/ ships a million rows of application identification. Wiring it in as a source needs a decision recorded first.
    CONFLICT {'step': "'Add a pytest fixture that spins a throwaway engine instance, applies every migration, and tears it down'", 'resolution': "The throwaway *engine* already exists — task 3's session-scoped `engine_dsn` fixture starts an in-memory RisingWave when nothing answers — and task 3 measured why a second one cannot be started per test: `single_node` binds fixed meta and compute ports (5690, 5688), so only one engine runs per machine. The new fixtures therefore make the throwaway unit a SCHEMA on that engine: `engine_schema` does CREATE SCHEMA / SET search_path / DROP SCHEMA ... CASCADE, and `migrated_engine` applies every migration into it. The engine is still throwaway (in-memory, gone at the end of the run) and the migrations still really execute; what is not per-test is the process. Rebuilding per-test process management would also have violated CONTEXT.md §3 ('use it, don't rebuild it')."}
    NEXT: Write your schema as the next numbered file, `sql/migrations/0002_<name>.sql`, and take the `migrated_engine` fixture from tests/conftest.py — it gives you a psycopg connection (autocommit, its own throwaway schema) with every migration applied, and `engine_schema` is the same thing without them. Do not create tables in a test by hand: a view a test queries should be the view a deployment gets, from the same file. Four things will bite otherwise. (1) Editing an applied migration is refused by checksum — including a whitespace or comment change — so amend by adding the next file, never by editing 0001. (2) RisingWave has no transaction around DDL, measured this session, so a file whose third statement fails leaves the first two behind and gets a `failed` ledger row that blocks every later run until a human clears it; keep migration files small. (3) A row is unreadable until `FLUSH`, and an INSERT onto an existing primary key silently overwrites. (4) The runner needs `autocommit=True` and says so rather than half-applying. `uv run scripts/migrate.py --status` / `uv run scripts/migrate.py` (or `make migrate`) is the by-hand path against the configured engine; docs/runbook.md §5 covers it, including the rule that matters for D1 onward: **migrate before data flows**, because the broker is consume-once and a view created after ingestion starts begins empty with no backlog to fill it from — only a replayed capture. sql/migrations/ deliberately holds nothing but the ledger; task 5's aggregation version is the first domain migration, and `test_the_ledger_constant_names_the_table_the_migration_creates` is the pattern for asserting a SQL constant equals its Python copy by asking the engine rather than by grepping the file. Still open and untouched: the concept/05 vs data/netify tension, bin/README.md's dangling reference to scripts/build-binaries.sh, and there is still no git repository, so nothing here was committed.

[2026-09-03 13:12:00 UTC] task 05 COMPLETED 723s exit=0 session=c44607c9-020d-4341-afd2-905237dfcbfa
    [D0 Foundations] Version registry for every recorded version dimension
    `helena.versions` now exists: `VersionSet` is a frozen Pydantic model of the eight dimensions concept/07-principles.md names (model, prompt, schema, rendering, taxonomy, enrichment snapshot, policy, aggregation), every one required with no default, whose field names are the column names — `stamp(row)` adds them and refuses a row that already carries one, `from_row(row)` is the replay direction and names any dimension the row does not record. The aggregation version exists twice, as `AGGREGATION_VERSION` and as the view `helena_aggregation_version` created by `sql/migrations/0002_aggregation_version.sql`, and a test asserts the two equal by applying the migrations to a throwaway engine and selecting from the view rather than by reading the file. The 'a revision is a new version module, never an edit' rule is written down in docs/decisions/0008-version-registry.md, the README and the runbook, with the bump procedure.
    NEXT: Stamp versions through `helena.versions` and nothing else. `VersionSet` requires all eight dimensions — model_version, prompt_version, schema_version, rendering_version, taxonomy_version, enrichment_snapshot_version, policy_version, aggregation_version — with no defaults, is frozen, forbids extras, hides the rejected input in validation errors (task 01's leak), and its field names ARE the column names: `versions.stamp(row)` returns a new dict with the eight columns added and refuses a row already carrying any of them, `VersionSet.from_row(row)` reads them back for replay and names what is missing, `as_columns()` is what goes into `log.info(..., versions=...)`. `VERSION_COLUMNS` is the tuple to declare in the migration that creates the first version-carrying table. Three things that will bite otherwise. (1) The aggregation version is in two places on purpose — `helena.versions.AGGREGATION_VERSION` and `sql/migrations/0002_aggregation_version.sql` — and tests/test_versions.py fails if they drift; bumping it is a NEW migration that drops and recreates the view (the runner's checksum refuses an edit to 0002), plus every aggregation view carrying the literal, in one change. docs/runbook.md §5 has the four-step procedure. (2) A streaming view CANNOT read that constant view — measured, three ways, all rejected as a nested-loop join — so a D2 aggregation view carries `'v1' AS aggregation_version` itself and needs its own test asserting the rows it produces equal AGGREGATION_VERSION. That per-view test is how the concept's 'asserted equal' invariant keeps holding once the literal spreads. (3) A revision is a new version module, never an edit: the taxonomy version and the agent schema versions get `v2` beside `v1` with `v1` left importable exactly as it was, and editing the taxonomy at all is an escalation. docs/decisions/0008-version-registry.md is the record. `helena.versions` was added to SUPPORT_MODULES in tests/test_package_layout.py — it is cross-cutting, not an eighth architecture component. Still open and untouched by this task: the concept/05 vs data/netify tension, bin/README.md's dangling reference to scripts/build-binaries.sh, and there is still no git repository, so nothing here was committed.

[2026-09-03 14:45:50 UTC] task 06 FAILED    217s exit=1 session=d295ed1b-03ec-4366-a80e-0dfbccdd3525
    [D1 Ingest] Flow record input contract and capture fixtures
    Session ended with exit code 1 and wrote no report.
    NEXT: The previous session produced no report. Check prds/logs/ before assuming nothing was changed on disk.

[2026-09-03 15:40:33 UTC] task 06 COMPLETED 852s exit=0 session=ca99ff18-a4c5-49c9-8d69-e6e5c8ab6e55
    [D1 Ingest] Flow record input contract and capture fixtures
    `helena.normalizer` now holds the raw flow record contract and the capture registry it is read from. `FlowRecord` is the input exactly as a producer supplies it — 15 Pydantic models, `strict=True` + `extra="forbid"` + `frozen=True`, no identity block — and `as_supplied()` round-trips a parsed record back to its JSON, asserted record by record over all 62 records of `data/ingest/flow-sample.jsonl` and both committed capture fixtures. `describe_capture` / `read_capture` / `scan_captures` address a capture by the sha256 of its file, refuse a fixture whose name and content disagree, and refuse a blank line rather than skipping it. Two capture fixtures under `tests/fixtures/captures/`, each named by its own digest, carry every layer combination in the sample plus the absence-is-not-emptiness cases. 116 new tests; the full suite is 291 passed.
    CONFLICT {'detail': "PRD step 4 says 'implement a capture registry that records file hash, record count and byte size'. The plainest reading — a persisted index of captures — is a second store, which `concept/instruction.md` §2 forbids ('no second database ... no checkpoint store ... no file-backed agent memory'), and an index that disagreed with the files would be worse than no index because the retained files ARE the durable record. Implemented as the compatible half: the registry computes a capture's description by reading it (`describe_capture`), and reads a directory of hash-named files (`scan_captures`). Nothing is written to disk. If a capture ever needs to be citable from a stored row, the row goes in the engine like every other durable row, which is the increment that first writes one — not this one.", 'resolution': 'implemented the compatible half; no state file, no index'}
    NEXT: `helena.normalizer` is now real and its maturity label is `experimental`. Read a capture through `describe_capture(path)` / `read_capture(capture)` / `scan_captures(directory)` and parse a record with `FlowRecord.model_validate_json(line)` — do not write a second reader, because replay must exercise the same path as live ingestion and a parallel one is what `concept/03-architecture.md` forbids. `read_capture` yields `(offset, raw bytes)` with `offset` the zero-based ordinal in the file and the line undecoded, which is what a quarantine row needs; it re-hashes the file and raises `CaptureError` if the content changed since the `Capture` was described, so do not hold a description across a write. Four things that will bite otherwise. (1) The contract is `strict=True` + `extra="forbid"` + `frozen=True`: any drift — an unknown key, a number sent as a string, a missing field — is a `ValidationError`, and NOTHING catches it yet. Task 8's adapter and task 9's quarantine are what turn it into a typed row; until then a bad record kills the caller. (2) The requiredness of every field comes from one 62-record capture and is the likeliest source of a false quarantine on real traffic — docs/decisions/0010-capture-identity.md has the rule and what to do about a high quarantine rate, and the answer is a new observation of the input, not a loosened field. (3) `as_supplied()` is `model_dump(exclude_unset=True)`, and the 62-record round-trip test is what enforces 'no invented fields' — adding a field to any model in the contract will fail 62 tests at once, which is deliberate: a field on the input contract that no producer sends is where a defaulted tenant comes from. (4) Capture identity is the sha256 of the FILE, so the same record in two files belongs to two captures with different references — which is why a raw-record reference is the capture plus an offset and never the record's own `id` (that is the producer's label, `udp.0`/`tcp.17`, unique only within a capture and scoped by protocol). The fixtures are `tests/fixtures/captures/<sha256>.jsonl` with a README mapping each digest to what it holds; every record in them is a byte-for-byte copy of a line of `data/ingest/flow-sample.jsonl`, and nothing in them is synthesised on purpose. Still open and untouched by this task: `bin/README.md` still points at a `scripts/build-binaries.sh` that does not exist, and there is still no git repository, so nothing here was committed. The concept/05-vs-data/netify tension that every session from task 0 carried forward is no longer open — `docs/decisions/0009-netify-application-identification.md` settled it by operator decision, so stop reporting it.

[2026-09-03 16:00:22 UTC] task 07 COMPLETED 674s exit=0 session=ffc7b4db-aca6-4948-bc84-60a3ac953f15
    [D1 Ingest] Normalizer core with identity stamping from configuration
    `helena.normalizer` now produces a `NormalizedEvent` — an `identity` block (tenant, sensor, schema version, event id, raw-record reference) beside an `observation` block that is still the record exactly as supplied. `Normalizer.from_settings(settings)` stamps the identity from deployment configuration only, refuses to exist without a non-blank tenant and sensor, and derives a deterministic event id as a sha256 over tenant, sensor, capture digest and record offset, so replaying a capture reproduces every id.
    CONFLICT {'step': 'Generate a deterministic event id from capture reference plus record offset so replay reproduces ids', 'detail': "Implemented as capture reference plus record offset PLUS tenant and sensor. Capture-plus-offset alone means two deployments ingesting the same capture file mint identical event ids in the single store, and task 4 measured that a RisingWave INSERT onto an existing primary key is an UPSERT that overwrites the row and raises nothing — so the narrower derivation is a silent cross-tenant overwrite that looks like it is working, which is the exact failure `concept/instruction.md` §6 and `concept/07-principles.md` name for a defaulted tenant. The step's replay property is unaffected: tenant and sensor come from configuration and do not change on a replay. The cost, stated in the code and in ADR-0011 rather than hidden: an id is reproducible only under the identity that made it, so re-ingesting a capture under a renamed tenant produces new ids.", 'resolution': 'Strengthened rather than skipped; recorded in docs/decisions/0011 and pinned by tests.'}
    CONFLICT {'step': 'Implement the normalized event model with an identity block (… schema version …)', 'detail': 'Not a conflict between documents, but a name collision between two contracts that both call a field `schema_version`. `EVENT_SCHEMA_VERSION` (helena.normalizer) versions the shape of an ingested event; `VersionSet.schema_version` (helena.versions) versions the agent output schema a stored assessment is replayed against. `concept/03-architecture.md` uses the phrase for the first and `concept/07-principles.md` for the second. Nothing joins them today; a test pins the collision so the increment where an event row first cites a version set finds it already known rather than discovering it in a column name.'}
    NEXT: Produce events through `Normalizer.from_settings(settings)` and nothing else — `normalize(capture, offset, line)` for one record, `normalize_capture(capture)` for a whole capture in file order, and do not write a second stamping path, because replay must exercise the same one as live ingestion. Five things that will bite otherwise. (1) `normalize` does NOT catch anything: an invalid record raises `ValidationError`, an offset outside the capture raises `CaptureError`, and a blank identity raises `ConfigurationError` at construction. Task 9's quarantine is what turns the first two into typed rows; until then a bad record kills the caller, which is deliberate. (2) The event id is a sha256 over tenant, sensor, capture digest and offset — WIDER than the PRD step said, because capture-plus-offset alone collides across tenants in a store where INSERT upserts silently (docs/decisions/0011). It is reproducible only under the identity that made it. (3) `EVENT_SCHEMA_VERSION` lives in `helena.normalizer` and has ONE home today; the increment that creates the first table holding a normalized event must add the SQL copy and the test asserting the two equal, the way `helena.versions.AGGREGATION_VERSION` and migration 0002 already do. Note that it is a different dimension from `VersionSet.schema_version` (the agent output schema) despite the identical field name — a row carrying both needs two column names. (4) The identity block is `tenant`, `sensor`, `schema_version`, `event_id`, `raw_record{capture_sha256, record_offset}`; those are the columns task 12 must carry into every flatten row, and `concept/03-architecture.md` is explicit that stamping them is a seam, not isolation enforcement — do not claim more. (5) There is still no engine table, no logging and no counter on this path, and capture identity remains provisional under live ingestion (ADR-0010), so every event id derived from an open file is provisional too. Still open and untouched: the concept/05 vs data/netify tension, bin/README.md's dangling reference to scripts/build-binaries.sh, and there is still no git repository, so nothing here was committed.

[2026-09-03 16:34:54 UTC] task 08 COMPLETED 1043s exit=0 session=0726384d-b64f-4a98-a575-af31878ea870
    [D1 Ingest] Per-format adapter interface
    `helena.normalizer` now reads its input through an adapter selected by `HELENA_INPUT_FORMAT`: `parse(line: bytes) -> FlowRecord | ParseFailure`, with no capture, no identity and no configuration, so an adapter cannot stamp a tenant, decide an event id or add a field to the event. Two are registered — `flow-json`, the format every real capture is in, and `flow-envelope`, a deliberately trivial synthetic second format — and a refused record now comes back as a typed `ParseFailure` (`malformed_json` / `not_this_format` / `contract_violation`) rather than as an exception, so a bad record does not stall the capture. 89 new tests; the suite is 396 passed.
    ESCALATION [configuration] One new configuration variable, `HELENA_INPUT_FORMAT`, required and without a default, added to `helena.config.REQUIRED_VARIABLES`, `.env.example`, the local `.env` (value `flow-json`) and ADR-0004. It is not on `concept/instruction.md` §3's escalation list — it is not a dependency, a store, an enrichment source, a contract field or an egress channel — and the task's own step 5 asks for adding an adapter to be a configuration change, so it was made rather than escalated. Recorded here because it changes what a deployment must set: an existing `.env` without it now fails at startup naming the variable, which is the intended direction.
    ESCALATION [contract] No contract field was added or changed — that is the point of the task, and a test asserts it. Recorded as a non-escalation so the next session does not go looking: `NormalizedEvent` is still `{identity, observation}` and `EventIdentity` still the same five fields.
    CONFLICT {'step': 'Define the adapter protocol: raw bytes/line in, validated normalized event or typed parse failure out', 'detail': "The adapter returns the validated OBSERVATION (`FlowRecord`), not a `NormalizedEvent`. Identity is assigned in exactly one place from deployment configuration (ADR-0011, `concept/02-concepts-and-taxonomy.md`), and an adapter that could assemble an identity would be one more place a tenant could come from — one per format, each able to get it wrong silently, which is `concept/instruction.md` §6's defaulted tenant. The step's stated shape still holds one level up: `Normalizer.normalize(capture, offset, line)` is bytes in, a validated normalized event or a typed parse failure out. Narrowing the adapter is what keeps that true for every future format.", 'resolution': 'Narrowed rather than skipped; recorded in docs/decisions/0012 and pinned by the test that the event contract has no place for a format.'}
    CONFLICT {'step': 'Implement a deliberately trivial second adapter over a variant fixture format', 'detail': 'Not a conflict but a tension with `concept/instruction.md` §1 — *no interface with one implementation, no registry for two things*. The second adapter is precisely what stops this from being a speculative abstraction: without it the boundary claim in `concept/06-technology.md` and `concept/07-principles.md` is untested and the interface is a shape nobody pushed against. It is recorded because the cost is real: `flow-envelope` is synthetic, no producer sends it, and it must be labelled that way wherever it appears so nobody later reads it as a supported input. It is so labelled in the module, in ADR-0012 and in the fixture README.', 'resolution': 'Built, and labelled synthetic in every place it is named.'}
    NEXT: Parse input through `Normalizer.from_settings(settings)` and nothing else — the adapter comes from `HELENA_INPUT_FORMAT` and constructing a `Normalizer` by hand now needs `adapter=` as well as `identity=`. Six things that will bite otherwise. (1) `normalize` NO LONGER RAISES on a bad record: it returns `NormalizedEvent | ParseFailure`, and so does every element `normalize_capture` yields. Code that assumes an event will happily read `.identity` off a failure — `isinstance(result, ParseFailure)` is the check, and `enumerate` over `normalize_capture` gives each result its offset because there is exactly one result per record in file order. `normalize` still RAISES `CaptureError` for an offset that addresses no record; that is this deployment's bug, not input drift, and quarantining it would file a producer's name against our own mistake. (2) `HELENA_INPUT_FORMAT` is a new required variable. It is in the local `.env` as `flow-json`, in `.env.example` and in `helena.config.REQUIRED_VARIABLES`; an environment without it fails at startup naming it. `helena.config` does not validate the value — `helena.normalizer.adapter_for` does, raising `ConfigurationError` naming the variable and listing the registered formats. (3) The three parse-failure reasons must stay distinct all the way into task 9's table and counter: `malformed_json` (framing broke), `not_this_format` (wrong adapter configured), `contract_violation` (producer drift — the rate ADR-0010 says to watch). They are not equally precise: `flow-json` cannot report `not_this_format` for another format's JSON object because it has no envelope to check, and a test pins that asymmetry. (4) Adding a third input format is an adapter class, an entry in `INPUT_ADAPTERS` and a configuration value — never a contract change, and a test asserts the event contract has no field naming a format. If a format ever needs one, that is an escalation. (5) `tests/fixtures/captures/` is `flow-json` only; the second format's fixture is in `tests/fixtures/captures-flow-envelope/` and dropping a non-`flow-json` file into the first directory fails a dozen contract tests, because `FIXTURE_LINES` sweeps it. `flow-envelope` is synthetic — nothing sends it, it exists so the boundary claim is measured — and it should keep that label wherever it is mentioned. (6) Still true from task 7 and unchanged: no engine table holds an event, `EVENT_SCHEMA_VERSION` still has one home and needs its SQL copy plus equality test in the increment that first stores an event, capture identity is provisional under live ingestion (ADR-0010), and there is still no git repository, so nothing here was committed. `bin/README.md` still points at a `scripts/build-binaries.sh` that does not exist.

[2026-09-03 17:11:49 UTC] task 09 COMPLETED 817s exit=0 session=2c4c6ec5-2ba8-4a13-8ad3-ed8b8ac23d21
    [D1 Ingest] Quarantine invalid input without stalling the stream
    Quarantine now lives in the engine. `sql/migrations/0003_ingest_quarantine.sql` creates `helena_ingest_quarantine` — ingestion identity, raw-record reference, the contract version that refused it, the typed ParseFailure and the payload as BYTEA exactly as read, keyed on (tenant, sensor, capture_sha256, record_offset) — plus the plain view `helena_ingest_quarantine_counts` grouped by reason. `helena.normalizer.Quarantine` writes and reads them and `Normalizer.ingest_capture(capture, quarantine)` is the ingestion path: it yields the events, routes every refusal to the table with its raw line, and carries on. `QuarantineCounts` joins the engine-side numerator to the capture's own record count and refuses a total that does not reconcile. 13 new tests, the engine-side ones executed against a real RisingWave; the suite is 409 passed.
    NEXT: Ingest a capture through `Normalizer.ingest_capture(capture, quarantine)` — it yields only `NormalizedEvent`s, writes every refusal to the engine with its raw line, and does not stop. `normalize_capture` still exists and still yields `NormalizedEvent | ParseFailure` for callers that want to see both; if you use it, you own the failures, and nothing stores them for you. Build the quarantine with `Quarantine(connection=<psycopg autocommit connection>, identity=settings.identity)`. Six things that will bite otherwise. (1) `ingest_capture` REFUSES a quarantine whose identity is not the normalizer's, with a `ConfigurationError` — the two are configured from the same `Settings` and that is the only combination that works, deliberately. (2) The quarantine primary key is (tenant, sensor, capture_sha256, record_offset); re-ingesting rewrites the same rows rather than doubling, so replay does not inflate the rate — do not add a timestamp or an event id to the row without reading ADR-0013 first, both were rejected on purpose. (3) `QuarantineCounts` refuses to exist if its numbers do not reconcile, and `by_reason` always carries all three reasons including zeros; `normalized` is `records - quarantined`, a subtraction that is exact only because `normalize_capture` yields one result per record — task 10's engine-side normalized count should reconcile AGAINST it, not replace it. (4) A rate over a capture with no records raises rather than returning 0.0. (5) Writes do not FLUSH; the two read methods do, so a count taken from a different connection mid-run may lag. (6) `CaptureError` (an offset addressing no record) still raises and is NOT quarantined — that is this deployment's bug, not the producer's. docs/runbook.md §6 is the operator's page for all of it, including what a rising `contract_violation` rate means and that the answer is a new observation of the input rather than a loosened field. Note the runbook's old '§6 When something is wrong' is now §7; nothing referenced it by number. Still true and unchanged: no event is stored in the engine, nothing consumes from the broker, `EVENT_SCHEMA_VERSION` still has one home (this table writes it as data, not as a SQL literal, so there is still no second copy to assert equal), capture identity is provisional under live ingestion (ADR-0010), `bin/README.md` still points at a `scripts/build-binaries.sh` that does not exist, and there is still no git repository, so nothing here was committed. One small stale statement noticed and left alone: `docs/decisions/0007-sql-migrations.md` says `sql/migrations/` holds the ledger and nothing else — true when it was written at D0, false since migration 0002, and it is a dated record rather than a live claim.

[2026-09-03 17:56:41 UTC] task 10 COMPLETED 1805s exit=0 session=7e7b65bd-5059-42e6-9179-46582ec2ae8c
    [D1 Ingest] Kafka wire-protocol ingress and egress plumbing
    The ingest path now runs over the wire: `helena.broker` is a thin producer/consumer wrapper that is the only module in the package importing a Kafka client, `helena.normalizer` consumes the ingest topic and writes validated events into `helena_normalized_events` (migration 0004), and `IngestCounts` reconciles four numbers from four places — the capture file, the run, `helena_ingest_counts` and `helena_ingest_quarantine_counts`. A capture published to a real blink topic, consumed back and stored in a real RisingWave produces rows identical to the same capture read from disk.
    ESCALATION [contract] The ingest topic's wire contract is new: the message value is the raw flow record byte for byte and the raw-record reference travels in two Kafka headers, `helena-capture-sha256` and `helena-record-offset`. This transports an existing contract field (RawRecordReference) rather than adding one, but it is a contract every producer sees, so it is recorded as a decision in docs/decisions/0014-the-ingest-topic-message.md rather than left implicit. The alternative — an envelope in the value — would have been a change to the input format for every producer, which docs/decisions/0012 says a second format must not require.
    ESCALATION [open-question] A message that carries no usable reference in its headers cannot be quarantined: a quarantine row is KEYED by capture and offset, so there is no row to write. It raises IngestMessageError and stops the run, naming the header. Where such a refusal would live if a deployment ever needed to keep them is left open rather than answered by inventing a second table or a dead-letter topic (the broker is not a store). Recorded in ADR-0014.
    ESCALATION [open-question] Live ingestion from a capture still being written is still not possible, and this increment inherits rather than resolves it: docs/decisions/0010 records that a hash identifies a capture only once the file closes, so a streaming producer has no digest to put in the header. Replay of a retained capture is fully served. The answer is a decision about what identifies a record in flight, not a provisional digest.
    ESCALATION [enrichment-source] The concept/05 vs data/netify tension noted in CONTEXT.md §3 was not touched by this task and remains open.
    CONFLICT {'step': 'Implement a thin producer and consumer wrapper taking broker address and topic NAMES (plural) from configuration only', 'conflict': "Only HELENA_INGEST_TOPIC was added. An output-topic variable would be a configuration key with one value and no reader — concept/instruction.md §1 forbids exactly that, and §3 puts a second egress channel behind a decision. The invariant the step is protecting ('the rule holds on both ends so replacing the broker is a configuration change') is enforced instead by tests/test_broker.py, which asserts that helena.broker is the ONLY module in the package importing a Kafka client — that covers sink.py as much as normalizer.py, today, before the sink exists. HELENA_OUTPUT_TOPIC enters with the sink that writes to it (task 46)."}
    CONFLICT {'step': 'The broker is memory-first, single-node, consume-once and restart-volatile, so a topic is never re-readable', 'conflict': "Not a conflict in substance, but concept/03-architecture.md's timing is wrong and the correction is recorded rather than papered over: the reclaim after a read is asynchronous, so an immediate second drain can still return every record. Documented in docs/runbook.md §3, ADR-0014 and tests/test_broker.py (which asserts both the reclaim and the never-read control)."}
    NEXT: The ingress path is: BrokerProducer.publish(topic, message.payload, message.headers()) -> BrokerConsumer.consume(topic) -> IngestMessage.from_headers(value=..., headers=...) -> Normalizer.ingest_messages(messages, events, quarantine) -> ingest_counts(capture=..., consumed=..., events=..., quarantine=...). The whole of it is exercised end to end in tests/test_normalizer.py against the real broker and engine; copy `publish()` and `consumed_messages()` from that file into the replay driver rather than reinventing the framing. Eight things that will bite otherwise. (1) HELENA_INGEST_TOPIC is a NEW required variable — it is in helena.config.VARIABLES, .env.example and the local .env, and any test that builds an environment dict must include it or Settings.load fails naming it. (2) `settings.infrastructure.ingest_topic` is where it lands; there is no output topic and adding one is task 46's. (3) `helena.broker` is the ONLY module allowed to import confluent_kafka — tests/test_broker.py fails on a second importer, on an unapproved name bound from it, on an HTTP or socket import inside broker.py, and on ANY address-shaped string literal anywhere in src/helena (including a `:9092`-style port). Put an address in configuration, and put a measured fact about the broker in a docstring (docstrings are excluded from that scan on purpose). (4) `create_topic` asks Metadata first — do not rewrite it as create-and-catch, blink answers a duplicate create with an unparseable response. (5) `publish` is asynchronous; nothing has reached the broker until `flush()`, which RAISES on outstanding or refused messages. The `with BrokerProducer(...)` block flushes on clean exit. (6) The reclaim after a drain is asynchronous — do not write a test asserting a topic is empty immediately, and do not build a retry that re-reads a topic. Replay publishes the retained capture. (7) `Quarantine.record` changed signature: it takes `reference=RawRecordReference(...)` now, not `capture=`/`offset=`. (8) `IngestCounts` refuses a set that does not reconcile and `complete is False` means records were lost between the producer and the store — the capture is still on disk, replay it. Migration 0004 creates `helena_normalized_events` (TABLE, PK tenant/sensor/capture_sha256/record_offset, observation JSONB) and `helena_ingest_counts` (plain VIEW, read by EventStore.normalized and the reconciliation). The flatten layer of task 12 builds over the table, not the view. Docs: docs/decisions/0014-the-ingest-topic-message.md is the wire contract and the broker-is-not-a-store measurements; docs/runbook.md gained §7 (ingest and the four counters) and its §3 now carries the corrected CreateTopics and reclaim measurements; the old §7 troubleshooting table is now §8 and has four new rows.

[2026-09-04 05:45:21 UTC] task 11 COMPLETED 1388s exit=0 session=01b49faa-854d-40dc-84ee-db49225c395d
    [D1 Ingest] Capture replay driver through the live ingestion path
    A retained capture can now be put back through the pipeline: `scripts/replay_capture.py --captures <dir> <sha256> [--rate N] [--ingest]` publishes the capture's records to the ingest topic in the sensor's own wire form, optionally runs the ingestion side in the same process, and prints the four reconciled counters. The two halves of the wire — `publish_capture` and `consume_ingest_topic` — live in `helena.normalizer` beside the code live traffic already uses, so there is one adapter, one `_stamp` and one pair of stores rather than a replay pipeline beside the real one.
    ESCALATION [interface] The PRD asks for `helena replay-capture <hash>`. There is no `helena` console entry point in this project and one was not added: a command dispatcher with exactly one subcommand is the 'registry for a second case that does not exist' `concept/instruction.md` §1 forbids, and `scripts/migrate.py` is the project's existing 'run it by hand' surface. The capability is identical and the argparse `prog` is `replay-capture`; the invocation is `uv run scripts/replay_capture.py`. If a second command is ever written, promoting both to a `helena` entry point is the increment that earns it.
    ESCALATION [configuration] The capture directory is a required `--captures DIR` argument rather than a new `HELENA_CAPTURE_DIRECTORY` variable. A required variable would fail startup for every deployment including the ones that never replay, and `Settings.load` resolves all or nothing; a CLI argument with no default keeps the fail-loud property where it belongs. This is recorded rather than assumed because 'where the retained captures live' is deployment knowledge, and the moment a second thing needs it — a live producer, an evaluation harness — it becomes configuration.
    ESCALATION [open-question] With `--ingest`, the replay process is also the consumer. That is correct today because nothing else consumes the ingest topic, but it means replay into a deployment that HAS a live consumer must be run without `--ingest` and the counters then come from whatever is consuming. The reconciliation is per capture and refuses a set that does not add up, so a topic carrying another producer's records during a verified run is reported as a failure to reconcile rather than folded into the numbers. Where a long-running consumer lives is still task 10's deferred item, not answered here.
    ESCALATION [enrichment-source] The concept/05 vs data/netify tension noted in CONTEXT.md §3 was not touched by this task and remains as ADR-0009 left it.
    CONFLICT {'step': 'Document replay as the procedure for backfilling a newly added view', 'conflict': "The procedure as the concept notes state it does not hold in this architecture, and the documentation says so rather than repeating it. `concept/06-technology.md` (and docs/runbook.md §5, which followed it) said a view added after ingest starts 'begins empty and stays that way ... there is no backlog to re-read it from'. Measured 2026-09-04: a materialized view created over `helena_normalized_events` backfills from the table and is maintained incrementally afterwards. The note's claim is true of a view over a stream; normalized events have been a table in the single store since migration 0004. `concept/` outranks the code, so this is reported as a discrepancy for the operator to settle in the note rather than fixed in `concept/`: docs/runbook.md §5 and §8 now carry the measurement and the narrower rule (what never reached the store cannot be recovered from the broker, only from the capture), and README says the same."}
    CONFLICT {'step': 'Implement `helena replay-capture <hash>`', 'conflict': 'See the first escalation. The command exists and does what the step asks; it is spelled `uv run scripts/replay_capture.py` because a one-subcommand dispatcher is the abstraction §1 forbids.'}
    NEXT: Replay is `uv run scripts/replay_capture.py --captures <dir> <sha256> [--rate N] [--ingest] [--idle-timeout S]`, and `tests/fixtures/captures/` is a capture directory (files are named by their own digest; `data/ingest/` is NOT one — flow-sample.jsonl fails the name check, which is deliberate). Underneath it are two new functions in `helena.normalizer`: `publish_capture(capture, producer, topic, rate=None)` creates the topic, publishes every record with the reference in its headers, flushes, and returns the count; `consume_ingest_topic(consumer, topic, idle_timeout=...)` is a generator turning wire messages into `IngestMessage`s and is the one place that decode happens. A live consumer daemon should call exactly those two, not reimplement the framing. Five things worth knowing before touching this. (1) A view added over `helena_normalized_events` BACKFILLS — measured, and it contradicts `concept/06-technology.md`; docs/runbook.md §5 and the new §8 carry the measurement, and the concept note itself has not been edited (concept/ outranks code, so that correction is the operator's). Task 12's flatten views therefore do not need to exist before data flows; what needs to exist before data flows is the events table. (2) `IngestCounts._reconciles` changed order: `consumed > records` is now checked first and says 'consumed more than once', because the upsert makes a double-consumed run arithmetically identical to a lossy one. No field changed and no caller changed. (3) `--ingest` makes the replay process the consumer too. That is right only while nothing else consumes the topic — which is the case today. (4) `--rate` is a floor, not a schedule, and a rate of zero or less is refused rather than read as 'unpaced'. (5) The runbook grew §8 (replay, the capture-directory rule, the two-replays-vs-two-publishes distinction, and the backfill procedure) and the old §8 troubleshooting table is now §9 with four new rows; README gained a 'Replay' subsection under ingest. Nothing else in the tree moved.

[2026-09-04 06:18:41 UTC] task 12 COMPLETED 1427s exit=0 session=fb17a8c2-bc28-42a1-9716-edab91e9e47f
    [D2 Context] Flatten-layer views over normalized events
    The bottom of the Context Builder's three view layers exists: sql/migrations/0005_flatten_layer.sql creates eight plain views over helena_normalized_events — flows, the DNS/TLS/HTTP layer-observation rows, and the unpacked DNS queries, DNS responses, HTTP requests and HTTP responses — each carrying tenant, sensor, capture sha256, record offset, event id and schema version on every row. Nothing is materialized, and the reason that is safe was measured rather than assumed.
    NEXT: The flatten layer is eight PLAIN views in sql/migrations/0005_flatten_layer.sql: helena_flatten_flows, _dns, _dns_queries, _dns_responses, _tls, _http, _http_requests, _http_responses. Read them, do not re-derive columns out of the JSONB — that is the duplication the layer exists to prevent. Every row starts with the same six identity columns (tenant, sensor, capture_sha256, record_offset, event_id, schema_version) in that order; tests/test_context.py::FLATTEN_SHAPES is the second copy of the layer's interface and is asserted against the engine, so adding a column to a view means editing that table too — deliberate friction. Three shape facts to build on rather than rediscover: helena_flatten_flows keeps the counters bidirectional and has no total column on purpose; helena_flatten_http_requests/_responses are HTTP/1 and HTTP/2 in one view with a `protocol` column, where a NULL exchange_number (and a NULL response content_length) is a property of HTTP/2 having no such field, not an unobserved value; and a layer-observation row's EXISTENCE is what says the layer was observed, so 'this flow observed DNS' is a row in helena_flatten_dns and never a flag on the flow. Task 13 should note that TUMBLE works directly off helena_flatten_flows.flow_start and that the sample splits 59/3 across 21:35:00Z. Nothing here is materialized and the signal layer above must not materialize a flatten view either — the measurement that a streaming MV over these views works is in tests/test_context.py::test_a_materialized_view_can_be_built_over_a_flatten_view, which is the evidence the decision rests on. src/helena/context.py is still Python-free and now says why, with maturity 'experimental'; docs/decisions/0015-the-flatten-layer.md carries the rest. Open from earlier sessions and untouched here: bin/README.md's dangling reference to scripts/build-binaries.sh, and the two remaining cross-cutting open questions in concept/08 (the record lost at a catch-up boundary, and durability for the single store).

[2026-09-04 07:34:03 UTC] task 13 COMPLETED 1760s exit=0 session=ceaeaa58-e432-431c-8724-7f51fad57f9e
    [D2 Context] Windowed host-context aggregation with bidirectional statistics
    The signal layer exists: sql/migrations/0006_host_context.sql creates helena_signal_host_context, a materialized view over helena_flatten_flows producing one row per (tenant, sensor, host, window start) from a five-minute tumbling window taken on the flow's start time. The host key is the source address, the four counters stay bidirectional with no total column, duration and flow count ride beside them, every row carries the aggregation version from the SQL literal and a context_id digest over the identity, the window and that version — and the row carries no verdict.
    CONFLICT {'kind': 'documents disagree', 'detail': "What a REVISION of a context is. concept/07-principles.md says 'A revised context is a new version, never an edit in place'. concept/08-open-questions.md lists as an assumption in force that 'context identity is stable across revisions, so a finding may cite an id whose numbers have changed', to be revisited when findings outlive a retention boundary. They cannot both describe one implementation. This increment implements the second: a late record folds into the existing context row, the counters change and the context_id does not — which is what an incrementally maintained view does, and concept/07 itself calls revision 'a property of the engine rather than something the project builds'. concept/07's own answer to the consequence is 'a context cited by a finding is copied out, never evicted', and nothing cites a context yet, so no copy-out is built. The measurement and the disagreement are recorded in the head of sql/migrations/0006_host_context.sql and in tests/test_context.py::test_a_late_record_revises_a_context_in_place rather than resolved by picking one note quietly. The task step 'revision producing a new version rather than an in-place edit' IS implemented for the other kind of revision — a revision of the aggregation itself: the aggregation version is inside the context_id digest, so a revised aggregation mints new context ids rather than changing what an existing id means, and the migration runner's checksum makes bumping it a new file rather than an edit.", 'blocking': False}
    NEXT: The signal layer is ONE materialized view: helena_signal_host_context in sql/migrations/0006_host_context.sql, columns in order context_id, tenant, sensor, host, window_start, window_end, flow_count, duration_seconds, bytes_sent, bytes_received, packets_sent, packets_received, aggregation_version. tests/test_context.py::HOST_CONTEXT_SHAPE is the second copy of that interface and is asserted against the engine, so adding a column means editing that tuple too — the same deliberate friction FLATTEN_SHAPES has. Five things to build on rather than rediscover. (1) It reads helena_flatten_flows and nothing else; the analytical layer must read THIS and never the flatten layer or helena_normalized_events. (2) It is the only materialized object in the three layers so far, and the flatten layer below it must stay plain — the 42 % measurement is why. (3) context_id = sha256(len-prefixed(tenant, sensor, host, window_start_epoch, aggregation_version)), computed in the engine; the test recomputes it in Python and the two agree byte for byte, so a caller that needs the id of a context it is about to look up can compute it without a round trip. The aggregation version IS in the digest and the event schema version is NOT in the event id — that asymmetry is deliberate and argued in both files. (4) A late record revises a context in place and the id does not change; concept/07-principles.md and concept/08-open-questions.md disagree about whether that is allowed, and the disagreement is recorded in the migration head and in invariant_conflicts above rather than resolved. Whoever builds findings owns the copy-out that freezes a cited context. (5) Window coherence is UNMEASURED and blocked on the corpus; the repository says so in the migration head, in README.md and in src/helena/context.py, and no claim stronger than 'the mechanism does what the note says' is made anywhere. Two hosts in one window is reachable only through a constructed capture — every sampled flow has the same source address — and test_two_hosts_in_one_window_are_two_contexts builds one by reversing a real record's address pair in a tmp_path capture. It is the only test that fails when the host is dropped from the group key, so do not delete it as redundant. Untouched and still open from earlier sessions: bin/README.md's dangling reference to scripts/build-binaries.sh, and the two cross-cutting open questions in concept/08 (the record lost at a catch-up boundary, and durability for the single store).

[2026-09-04 08:12:49 UTC] task 14 COMPLETED 2324s exit=0 session=9393e5b7-cf62-4c99-91a0-c7e4e4736d1a
    [D2 Context] Entity extraction with observation-scoped traffic
    The signal layer's second output exists: sql/migrations/0007_context_entities.sql creates helena_signal_entity_observations (a plain view, one row per flow-and-entity observation) and helena_signal_context_entities (a materialized view, one row per entity per context). Address, domain, fingerprint and url entities are extracted from the places concept/05-threat-intelligence.md names, each row carries five flags saying which layer observed the value and the bidirectional traffic of the distinct flows that observed it, and context_id and aggregation_version are read off the host context rather than recomputed.
    CONFLICT {'kind': 'documents disagree', 'detail': "How many entity types there are. concept/02-concepts-and-taxonomy.md's glossary defines an entity as 'a `domain`, an `address`, or a TLS `fingerprint`' -- three. concept/05-threat-intelligence.md's 'What can be enriched' table has four, adding `url` extracted from HTTP and HTTP/2 URIs, and prd task 14 lists four. Four are implemented, because 05 is the specific note about what enrichment joins to, it names the source of every one of the four, and the url gap it records ('URL feeds have narrow reach ... TLS yields SNI, not a URL') only makes sense if url rows exist. The glossary line in 02 looks like an omission rather than a decision, but correcting concept/ is the operator's call, not a session's.", 'blocking': False}
    NEXT: The entity rows are helena_signal_context_entities in sql/migrations/0007_context_entities.sql, columns in order: context_id, tenant, sensor, host, window_start, window_end, entity_type, entity_value, fingerprint_algorithm, observed_as_flow_destination, observed_in_dns_query, observed_in_dns_response, observed_in_tls, observed_in_http, observed_flow_count, observed_bytes_sent, observed_bytes_received, observed_packets_sent, observed_packets_received, aggregation_version. tests/test_context.py::CONTEXT_ENTITIES_SHAPE is the second copy of that interface and is asserted against the engine, so adding a column means editing that tuple too -- the same deliberate friction FLATTEN_SHAPES and HOST_CONTEXT_SHAPE have. Six things to build on rather than rediscover. (1) The grain is one row per (context_id, entity_type, entity_value) and it is asserted two ways: no duplicate key, and the whole row set equal to the same set rebuilt from the JSON. (2) The traffic columns are observation-scoped -- the traffic of the flows in which the entity was observed, summed over DISTINCT flows -- and they are not traffic to the entity. Do not rename them to something that reads as if they were, and do not add a total: the four stay bidirectional. (3) The five flags are the vocabulary the rendering gets. observed_as_flow_destination is the composition rule's scope test for addresses; for domains there is no scope test and the other four flags are the weaker substitute concept/02 records, so a rendering may say 'this name was an SNI' and may not say 'this name received bytes'. (4) context_id and aggregation_version come from helena_signal_host_context through an equi-join -- there is no second digest and no second version literal in 0007, and the join is what pins the duplicated INTERVAL '5 minutes'. (5) The layering now has three objects in the signal layer and tests/test_context.py::test_every_signal_object_declares_what_it_is is a prefix query, so an object added to the layer without a declaration fails there (that test is task 13's, widened from the single host-context assertion and renamed). The analytical layer above must read helena_signal_host_context and helena_signal_context_entities and never the flatten layer or helena_normalized_events. (6) Where prd task 15 applies: the domain branch of helena_signal_entity_observations is the one place a name becomes an entity value, so registrable-domain normalization has exactly one seam -- and ADR-0009 constrains it to match on the name as observed. Untouched and still open from earlier sessions: bin/README.md's dangling reference to scripts/build-binaries.sh, and the two cross-cutting open questions in concept/08 (the record lost at a catch-up boundary, and durability for the single store).

[2026-09-04 09:08:05 UTC] task 15 COMPLETED 3310s exit=0 session=d510bf18-256d-4aaf-9722-8002a7d1e50c
    [D2 Context] Registrable-domain normalization via the Public Suffix List
    The Public Suffix List is loaded and the registrable domain is derived in the engine. sql/migrations/0008_public_suffix_list.sql adds helena_reference_public_suffix (one snapshot at a time, one row per match key) and helena_reference_public_suffix_load (one row per attempt, three statuses, three typed failure reasons), plus four signal-layer objects that turn a domain entity value into its public suffix and registrable domain by an equi-join against the rules. src/helena/enrichment.py stops being a placeholder: it fetches, parses and replaces a snapshot, and records the attempt either way; scripts/load_public_suffix_list.py is what runs it on a schedule and is the one place the source URL is written.
    ESCALATION [source] A source is now fetched and stored that was not before: publicsuffix.org/list/public_suffix_list.dat. concept/05-threat-intelligence.md lists the Public Suffix List in the source catalogue but below the two accepted rows, i.e. inside the block the note calls deferred -- while giving it an empty 'Maps to' cell and no tier, which is what the note means by it not being an enrichment source at all. prd task 15 instructs the load, so it was done; the operator may want the catalogue to say explicitly that the normalization row is not governed by the same 'deferred' as the feeds below it. No credential is involved (the list is open) and no runtime dependency was added -- the fetch is urllib from the standard library, because docs/decisions/0002-dependency-set.md keeps requests and httpx deliberately absent.
    ESCALATION [contract] helena.versions.VersionSet has eight dimensions and none of them is a normalization snapshot. `enrichment_snapshot_version` is the feed snapshot an enrichment join matched against; the Public Suffix List snapshot is not that, and a registrable domain that reached an assessment would have no dimension to record what decided it. Nothing cites one yet, so nothing is wrong today. The derived row carries `public_suffix_snapshot_version` so the fact is available; adding a ninth dimension to the version set is a contract change and therefore the operator's, not a session's. This becomes real in the increment that first renders a registrable domain to an agent.
    NEXT: Migration 0008 adds seven objects. Two tables with a writer -- `helena_reference_public_suffix` (snapshot_version, rule, suffix, is_wildcard, is_exception, section; PK (rule, suffix)) and `helena_reference_public_suffix_load` (attempted_at, source_url, status, snapshot_version, rule_count, failure_reason, failure_detail; PK (attempted_at, source_url)) -- plus `helena_reference_public_suffix_load_counts`, a plain view mirroring helena_ingest_quarantine_counts. Then four signal objects: `helena_signal_domain_suffix_candidates` and `helena_signal_domain_public_suffix` (plain intermediates), `helena_signal_domain_registrable` (materialized: observed_name, normalized_name, name_label_count, public_suffix_label_count, public_suffix, registrable_domain, registrable_domain_status, public_suffix_snapshot_version) and `helena_signal_context_domains` (plain: every entity column except fingerprint_algorithm, plus the five derived ones). Seven things to build on rather than rediscover. (1) `entity_value` is untouched and must stay so -- ADR-0009 fixes the one existing feed's join on the name AS OBSERVED, and the registrable domain is a column beside it for a different question, never a rewrite of it. (2) `registrable_domain_status` has four values and they are four different things: `derived`, `name_is_a_public_suffix` (nothing is missing; there is nothing there), `invalid_name` (the list was consulted and refused the name) and `list_not_loaded` (the reference table is empty -- `missing`, never `no_match`). A consumer that collapses any two of them into a NULL breaks concept/instruction.md §2. (3) The table holds exactly one snapshot at a time and the derivation GROUPs BY snapshot_version rather than taking max(), so a two-snapshot table produces two rows per name and fails the one-row-per-name assertion loudly instead of being silently decided by whichever digest sorts higher. (4) tests/test_context.py::test_every_signal_object_declares_what_it_is is still a prefix query and now names seven objects; adding one to the signal layer means editing that dict. tests/test_enrichment.py holds REGISTRABLE_SHAPE, REFERENCE_TABLE_SHAPE and LOAD_TABLE_SHAPE as the second copy of the three interfaces, asserted against the engine -- the same deliberate friction FLATTEN_SHAPES and HOST_CONTEXT_SHAPE have. (5) `helena.enrichment` is where the next loader goes, and the shape to copy is: fetch, parse COMPLETELY, then write; a typed PublicSuffixListError carrying one of three reasons; a frozen Pydantic load row that refuses a failure naming a snapshot or a success naming a reason; insert-then-FLUSH-then-delete-by-old-version; and the source URL through Redactor.url before it is stored as provenance. The redactor parameter is required rather than optional, deliberately. (6) The source URL lives in scripts/load_public_suffix_list.py and nowhere else -- the package may not hold one, and test_no_module_in_the_package_holds_a_broker_address is what enforces that. The next feed's URL belongs in its own script for the same reason. (7) The suite now makes two live HTTP requests to publicsuffix.org and skips them when the machine has no route. If a future session sees test_the_extract_answers_as_the_published_list_does fail, the committed 34-rule extract is stale and needs regenerating from the current snapshot -- that is what the failure means, not that the derivation is wrong. Untouched and still open from earlier sessions: bin/README.md's dangling reference to scripts/build-binaries.sh, and the two cross-cutting open questions in concept/08 (the record lost at a catch-up boundary, and durability for the single store).

[2026-09-04 10:12:35 UTC] task 16 COMPLETED 3868s exit=0 session=0a5b9b6b-3328-4a11-aef2-7f13230979e2
    [D2 Context] Completeness, context versioning and the retention boundary
    sql/migrations/0009_retention_boundary.sql puts a retention boundary around the context views — two retained materialized views under one 24-hour temporal filter, a live plain view carrying `completeness` (`open`/`provisional`, no `final`) and a `context_version` digest, a rejection counter over the unbounded aggregate, and `helena_frozen_context`, the copy-out table. helena.context gains its first Python: ContextStore.freeze (copy a cited context out before it is evicted, typed refusal when it is already gone), ContextStore.frozen, and ContextStore.rejections with a rate that refuses to be 0.0 over no records.
    ESCALATION [parameter] The retention horizon is 24 hours in the code and it is a CANDIDATE, not a decision. concept/08-open-questions.md records the horizon as unset and to be chosen by watching the rejection counter, so a value had to exist for the boundary to be built at all. It is labelled as a candidate in the migration head, in helena.context.RETENTION_HORIZON's comment, in the README and in concept/08, and changing it is a new migration. The operator may want to set it deliberately once real traffic has been through helena_signal_retention_rejections.
    ESCALATION [configuration] The task says 'one configured horizon'. It is NOT an environment variable, deliberately: a streaming temporal filter takes a literal, so an env var would be a value the running view ignores — the silent-configuration-default trap in instruction.md §6, wearing the opposite mask. It is a project constant with an engine-side copy, changed by a migration, with all three homes asserted equal by execution. If the operator wants deployment-time configuration of the horizon, that is a decision about generating or parameterising migrations and it is not made here.
    CONFLICT {'step': 'Add a test that a revised context produces a new version rather than editing in place', 'conflict': "As written this contradicts concept/07-principles.md as it now stands. The note was corrected on 2026-09-04 — 'A revised context keeps its identity: the counters change in place and the context_id does not' — after task 13 measured that an incrementally maintained view edits in place, and concept/08 settles the same thing. Authority order puts concept/ above the task, so the step was not implemented as written.", 'resolution': "The compatible half was implemented and is what the tests assert: identity stable, VERSION new. `context_version` is a digest of the context id and its six statistics, so a revision leaves context_id alone and mints a new version — 'a revised context is a new version rather than an edit in place' is true of the thing a citation records, which is the property the step was after. test_a_revised_context_mints_a_new_version_and_keeps_its_identity asserts both halves, and test_a_frozen_context_survives_the_revision_of_the_context_it_copied shows what it buys: the copy taken before the revision still says what the citation was issued against."}
    CONFLICT {'step': 'Record as untested and not to be inferred whether a late record inside the boundary still revises under a temporal filter', 'conflict': 'instruction.md §4 forbids inferring a behaviour that can be tested, and this one can be tested. Recording it as untested when the repository can measure it would have left a note that is wrong.', 'resolution': 'Measured instead, and the concept note updated to record the measurement, the test that holds it, and what is still NOT covered — a record arriving for a window that has just left the horizon, which is a boundary crossing this fixture set cannot construct without waiting out a real horizon.'}
    NEXT: The signal layer now has a boundary above it, and the next thing built on a context should read helena_signal_host_context_live (bounded, plus completeness and context_version) rather than helena_signal_host_context — the unbounded aggregate is now only for the rejection counter and for replay of archived captures. Two practical consequences. FIRST: over every fixture in this repository the retained views are EMPTY, because the fixtures are dated 2024-06-01 and the horizon is 24 hours; a test that needs a live context must re-stamp `ts` on a real record (tests/test_context.py::restamped and ::current_window are there for it), and a test that queries a retained view over the plain layer capture will correctly see nothing. SECOND: helena.context is no longer SQL-only — ContextStore is the writer for helena_frozen_context and the reader for the rejection counter, and the module docstring's table lists what each migration contributes. The horizon is a candidate at 24 hours, not a decision; if a session wants to change it, that is a new migration that drops and recreates all four views and the table, and helena.context.RETENTION_HORIZON has to move with it — the suite asserts the three copies equal by asking the engine, including the interval RisingWave stored in the retained view's own definition, so a partial change fails loudly. Task 17 is the view-layering boundary tests and materialization policy: the objects added here declare themselves in tests/test_context.py::test_every_signal_object_declares_what_it_is (prefix query over helena_signal_%) and ::test_the_retention_objects_declare_what_they_are, and the two retained views are the first materialized views in the layer whose reason to be materialized is state eviction rather than being a join target — worth saying explicitly in whatever policy that task writes down.

[2026-09-04 11:40:31 UTC] task 17 COMPLETED 1998s exit=0 session=f1fd5cf3-efb2-421e-a9a9-46f200e03a5d
    [D2 Context] View-layering boundary tests and materialization policy
    Every object the migrations create now declares itself above its CREATE -- Layer / Object / Reads / Read by -- and helena.migrations.declarations() refuses a file that does not, refuses a Reads: that is prose, refuses an Object: that disagrees with the CREATE under it, and refuses a MATERIALIZED VIEW whose Read by: names nobody. tests/test_view_layering.py (21 tests) turns the flatten -> signal -> analytical rule into something that fails: it asserts every Reads: line equal to rw_catalog.rw_depend on a running engine before drawing any conclusion from a comment, then applies helena.migrations.MAY_READ over those verified edges. scripts/dev_check.py --storage (make storage) reports what each relation of a migrated schema stores, so the materialization cost is read off the engine instead of argued.
    CONFLICT {'step': 'Write a test that parses the migration SQL and asserts no analytical view references the flatten layer or a source directly', 'conflict': "prds/CONTEXT.md is explicit that a test asserting on SQL text is not a test -- it finds the comment arguing for a thing's absence -- and instruction.md §7 requires SQL to be exercised by execution. A parser over the .sql files alone would have been exactly the forbidden shape, and worse here than usual, because the thing parsed IS a comment.", 'resolution': 'Both, in order. The declaration comments are parsed from the files (they are the only place a layer label can live -- the engine strips comments), and then test_the_declared_reads_are_the_dependencies_the_engine_recorded proves the parsed `Reads:` edges equal rw_catalog.rw_depend on a running engine, expanded through plain views. The layering rule is then applied to edges the engine has agreed with. A comment that drifted from its SQL fails at the engine comparison before it can make the layering test agree with a lie.'}
    CONFLICT {'step': 'Document the measured cost -- a materialized intermediate feeding only an aggregate cost 42% more disk', 'conflict': "instruction.md §5 forbids claiming more than was demonstrated and forbids generalizing a measurement. Repeating '42 %' as this repository's number would have been repeating someone else's measurement as ours; measuring it here and reporting the result as '42 %' would have been worse.", 'resolution': "Measured here, twice, and reported as what it is: +12 % and +56 % on two inputs, with the reason they differ (the aggregation factor) stated. concept/03-architecture.md's 42 % is cited as a third workload's number that this repository does not reproduce, and the note is NOT edited -- it records a real measurement from elsewhere, and correcting it would need the workload it was taken on. What the test asserts is the direction."}
    NEXT: The layering rule is now enforced and the enforcement is in helena.migrations, not in the tests: declarations() parses the four-field block above every CREATE, layering_violations() applies MAY_READ, reads_the_engine_records() expands the declared edges the way the engine does. tests/test_view_layering.py is where all of it fails. Four things to know before adding an object. (1) YOUR CREATE NEEDS A DECLARATION BLOCK immediately above it, no blank line, with Layer / Object / Reads / Read by. `Reads:` must be a comma-separated list of relation names or the word `nothing` (a note may follow the first sentence); `Read by:` is prose, and a relation is recognised in it by the helena_ prefix. A materialized view whose Read by: names nobody is refused. (2) YOU MUST ALSO EDIT THE FILES YOU READ FROM: every relation you declare in `Reads:` has to name you in its own `Read by:`, or test_every_relation_that_reads_an_object_is_named_in_its_read_by fails. That is deliberate friction and it is how the old files stopped being stale -- five of them were. (3) The layers are operational / source / reference / flatten / signal / analytical, and the first analytical object may read signal and reference only. Adding a layer means adding a row to MAY_READ, which a test asserts. (4) DO NOT EDIT AN APPLIED MIGRATION, including its comments -- the checksum refusal is real and was measured this session; the retrofit of 0001-0004 and 0008 was the last free one, and any store migrated before it has to be dropped and re-migrated. For the cost side: `make storage` prints what each relation stores, a plain view never appears with a number, and docs/decisions/0016-view-layering-and-materialization-policy.md carries the measurements and says plainly that the concept note's 42 % is not a rate this repository reproduces.

